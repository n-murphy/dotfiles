---
extends: default

rules:
  # 1000 chars should be enough, but don't fail if a line is longer
  line-length:
    max: 1000
    level: warning

  # accept both     
  # key:
  #   - item
  #
  # and
  # 
  # key:
  # - item
  indentation:
    indent-sequences: whatever